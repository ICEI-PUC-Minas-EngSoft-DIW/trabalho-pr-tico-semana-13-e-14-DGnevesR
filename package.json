{
  "name": "zwitter",
  "version": "2.1.0",
  "scripts": {
    "start": "json-server --watch db/db.json --port 3000",
    "dev": "concurrently \"npm run start\" \"live-server public --port=8080\""
  },
  "dependencies": {
    "json-server": "^0.17.4"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "live-server": "^1.2.2"
  }
}